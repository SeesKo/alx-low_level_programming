#include <stdio.h>

/**
 * main - the starting point for the program's execution
 * Return: 0 means successful execution
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
